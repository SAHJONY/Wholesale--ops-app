fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.1
psycopg[binary]==3.2.13
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
python-multipart==0.0.20
pytest==8.3.4
svix>=1.75.0,<2.0.0

# app/decision_intelligence.py imports this lazily and degrades to its
# deterministic path if the import fails, so the API boots without it. It is
# declared because that degradation is silent from the operator's side: with
# ANTHROPIC_API_KEY set and the SDK missing, analyses would quietly come back
# labelled source="deterministic" instead of using the model.
anthropic==0.120.0

# The second engine, for the same reason: with OPENAI_API_KEY set and the SDK
# missing, a Claude outage would fall through to deterministic analysis while
# the configuration says a fallback model is available.
openai==2.15.0

# upstash-redis stays removed: still imported nowhere. On a serverless deploy
# an unused dependency is pure cold-start, bundle-size and CVE surface.
