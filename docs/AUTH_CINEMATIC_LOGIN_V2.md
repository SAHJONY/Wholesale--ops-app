# Owner Access + Cinematic Login V2

This delivery aligns the owner-access proxy with the unified Vercel Services backend transport and adds a branded cinematic login visual layer.

## Runtime fix

`/api/owner-access/*` now resolves `BACKEND_INTERNAL_URL` before the legacy external backend URL, matching the canonical `/api/backend/*` proxy and the single-project Vercel Services architecture.

## Visual layer

The login remains a secure owner-access surface while adding an original SAHJONY wholesale acquisition landscape. The artwork references the operating workflow without exposing seller PII or a live deal address:

- public distress / source evidence
- ARV / repairs / MAO / buyer spread
- seller → contract → disposition → close
- Houston · Nationwide operating context

The existing HttpOnly owner session, authentication endpoints, permissions and execution gates are preserved.